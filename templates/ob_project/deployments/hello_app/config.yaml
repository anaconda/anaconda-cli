name: hello-app
port: 8000
auth_type: Browser
commands:
  - pip install -r deployments/hello_app/requirements.txt && uvicorn deployments.hello_app.app:app --host 0.0.0.0 --port 8000
resources:
  cpu: '0.5'
  memory: 512Mi
